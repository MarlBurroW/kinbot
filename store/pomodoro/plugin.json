{
  "name": "pomodoro",
  "version": "1.0.0",
  "description": "Pomodoro technique timer. Start focused work sessions with timed breaks to boost productivity.",
  "author": "KinBot Team",
  "license": "MIT",
  "main": "index.ts",
  "icon": "🍅",
  "tags": ["productivity", "timer", "tools", "focus"],
  "permissions": [],
  "config": {
    "workMinutes": {
      "type": "select",
      "label": "Work Duration",
      "description": "Duration of each work session in minutes",
      "options": ["15", "20", "25", "30", "45", "50", "60"],
      "default": "25"
    },
    "shortBreakMinutes": {
      "type": "select",
      "label": "Short Break",
      "description": "Duration of short breaks in minutes",
      "options": ["3", "5", "10"],
      "default": "5"
    },
    "longBreakMinutes": {
      "type": "select",
      "label": "Long Break",
      "description": "Duration of long break (after 4 pomodoros) in minutes",
      "options": ["15", "20", "25", "30"],
      "default": "15"
    },
    "longBreakInterval": {
      "type": "select",
      "label": "Long Break After",
      "description": "Number of pomodoros before a long break",
      "options": ["3", "4", "5", "6"],
      "default": "4"
    }
  }
}
